#include <iostream>

using namespace std;

int main()
{
    float base, altura, area;
    cout<<"Inscribir la base: "
    cin>> base;
    cout<<"Digitar la altura: ";
    cin>>altura;
    area=(base*altura)/2;
    cout<<"El area es: " <<area;
    
    return 0;
}
